<h1>Movies#index</h1>

<table>
  <thead>
      <tr>
        <th>タイトル</th>
        <th>公開年</th>
        <th>概要</th>
        <th>url</th>
        <th>上映中？</th>
        <th>登録日</th>
        <th>更新日</th>
      </tr>
    </thead>
    <tbody>
      <% @movies.each do |movie|%>
      <tr>
        <td><%= movie.name %></td>
        <td><%= movie.year %></td>
        <td><%= movie.description %></td>
        <%= image_tag movie.image_url, class: "movie-image"  %>
        <td><%= movie.is_showing %></td>
        <td><%= movie.created_at %></td>
        <td><%= movie.updated_at %></td>
      </tr>
      <% end %>
    </tbody>
</table>
