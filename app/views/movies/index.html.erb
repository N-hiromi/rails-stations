<div class="form">
  <h2>映画検索</h2>
  <% flash.each do |message_type, message| %>
    <%= message %>
  <% end %>

  <%= search_form_for @q, url: "/movies" do |f| %>
    <%= f.search_field :name_or_description_cont, placeholder: '検索ワード' %>
    <div class="radio-form">
      <%= f.radio_button :is_showing_eq, "nill", {:checked => true} %>
      <%= f.label :is_showing, "全て" %>
      <%= f.radio_button :is_showing_eq, true %>
      <%= f.label :is_showing, "上映中" %>
      <%= f.radio_button :is_showing_eq, false %>
      <%= f.label :is_showing, "公開予定" %>
    </div>
    <%= f.submit '検索', class: 'btn btn-primary' %>
  <% end %>
</div>

<table>
  <thead>
      <tr>
        <th>id</th>
        <th>タイトル</th>
        <th>公開年</th>
        <th>概要</th>
        <th>画像URL</th>
        <th>上映中</th>
        <th>登録日</th>
        <th>更新日</th>
      </tr>
    </thead>
    <tbody>
      <% @movies.each do |movie|%>
      <tr>
        <td><%= movie.id %></td>
        <td><%= movie.name %></td>
        <td><%= movie.year %></td>
        <td><%= movie.description %></td>
        <td><%= image_tag movie.image_url, class: "movie-image"  %></td>
        <td><%= movie.is_showing %></td>
        <td><%= movie.created_at %></td>
        <td><%= movie.updated_at %></td>
      </tr>
      <% end %>
    </tbody>
</table>

